
#include <iostream>

int main(int argc, char **argv)
{
    std::cout << argc;
    return 0;
}

//binary is compiled file/program, inside cmkae build debugm use ./app name to run it 
//need to rebuild changes before running on terminal (play on GUI is build and run), builds whats selected 
//argv: first =  name of file
//argc: arg count


